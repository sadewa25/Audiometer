package com.codedirect.pulmos.utils.dummies

import com.codedirect.pulmos.utils.models.Menus

object DataMenu {
    fun getData(): ArrayList<Menus> {
        val menus = arrayListOf<Menus>()
        return menus
    }
}