package com.codedirect.audiometer.utils.dummies

import android.content.Context
import com.codedirect.audiometer.R
import com.codedirect.audiometer.utils.models.Menus

object DataMenu {
    fun getData(): ArrayList<Menus> {
        val menus = arrayListOf<Menus>()
        return menus
    }
}