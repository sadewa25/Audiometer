package com.codedirect.elbicare.utils.dummies

import com.codedirect.elbicare.utils.models.Menus

object DataMenu {
    fun getData(): ArrayList<Menus> {
        val menus = arrayListOf<Menus>()
        return menus
    }
}